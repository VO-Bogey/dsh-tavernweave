# 更新记录（CHANGELOG）

> 本文件记录每个对用户可见的版本变化与发布状态。
> 版本命名：`0.x` 为快速开发期；`beta` 为真实验证期；去掉后缀表示该轮真实验证通过；`v1.0.0` 起为本项目首个正式版。

## v1.0.0 · 首个正式版（2026-09-03）

状态：首个正式版。真实 DSH 会话验收全部通过；制卡核心链路（材料登记 → 会话内写卡 → JSON 导出 → 真实 SillyTavern 导入运行）真机验收通过；状态栏生成代码的真机验收通过。

### 本版新增

- **材料登记到写卡**：TXT/Markdown 自动切章大纲、每章“复制章节任务”（来源片段 + 事实/推断两步）、按材料 SHA-256 断点续接。
- **卡内状态栏生成**：从角色卡真实变量（`extensions.variables/mvu`、世界书 `[initvar]`）提取变量模型，按真实变量生成 HUD 预览；可复制“替换正则 JSON”（裸 HTML）与“`{{getvar}}` 状态行”；无变量时自动降级为文字状态行。
- **PNG 角色卡导入**：解析 PNG tEXt 块（`chara`/`ccv3`，UTF-8 安全），导入后可直接盘点结构。
- **资料库分组浏览**：按工程检查单 / 卡片格式 / 变量 / 前端 / 运维 / 生态 / 概念分类浏览原作者指南。
- **前端质感打磨**：按钮按压反馈与焦点环、强调色与状态色单一真源（浅色主题自适应）、弹出层入场动效、圆角刻度、面板拖拽边界回弹。
- **修复**：工作台打开报 `usage is not iterable`；技能状态与口令速查高频刷新（改为低频轮询 + 缓存）。

### 兼容声明

- 支持 DSH `0.1.1-rc.2`（静态接口面审核）与 `0.1.2-alpha.2`（真机验证基线）。
- **不兼容** DSH `0.1.2-alpha.4+`：该版本将 Session API 由 `events` 改为 `snapshotEvents`，本项目技能状态重放会失效；升级前需完成兼容适配（详见 `docs/dsh-native-adapter.md`）。

### 已知边界

- 模拟酒馆按项目约定暂缓（后续可选扩展）。

## v0.3.0-beta.2 · 统一排期验证版

本版把后续工作合并为一条统一排期，每一阶段对应插件用户能看到、能操作、能验收的结果；全部阶段已在 v1.0.0 收口。

### 本版新增

- 会话工具栏、工坊进度胶囊与 Session 日志并排显示，联动 DSH 原生 Goal / Todo / Skill 活动。
- 卡内状态栏静态预览（桌面 / 窄屏切换）与「从材料开始」本地登记预览（字数、章节、分片数、SHA-256 摘要）。
- 资料库检索、全文打开与错误提示；更新中心安全更新检查（不覆盖本地未提交改动）。
- 制卡操作指南置顶口令速查；Soul 三头像与口令速查的一键插入。

## v0.3.0-beta.1 · 第一版验证版

状态：可实际使用，等待真实 SillyTavern 角色卡链路验收。

### 本版新增

- 接入 DSH 0.1.2 原生 Agent preset 与 TavernWeave 20 个 Skill。
- 会话内「✦ 酒馆」入口、制卡工具栏、Soul 口令插卡；工具栏位于输入框上方并与输入框宽度对齐。
- 口谕速查、Skill 状态、原作者资料库、JSON 角色卡盘点与组件草稿编辑。
- 本地角色卡项目记录（来源、阶段、版本、草稿、验收状态）。
- 工作区白名单、面板位置、默认 Soul 人格与更新检查设置。
- 新用户引导、面板内提示与制卡工作流说明。

### 已知边界

- 工作台当时只接收 JSON；PNG 载荷与 TXT 材料导入未实现。
